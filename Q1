
#include <iostream>
using namespace std;
int main() {
    double a, b, c;
    // Input three marks
    cin >> a >> b >> c;
    // Calculate average
    double average = (a + b + c) / 3;
    // Output the average
    cout << "Average marks: " << average << endl;
    return 0;
}
