#include <iostream>
using namespace std;

int main() {
    float radius;
    const float PI = 3.14159f;  // 'f' suffix for float

    cout << "Enter the radius of the fountain: ";
    cin >> radius;

    float area = PI * radius * radius;

    cout << "Area of the fountain is: " << area << endl;

    return 0;
}
